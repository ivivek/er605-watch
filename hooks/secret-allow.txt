# Per-repo allowlist for the secret-scanning git hooks (hooks/secret-scan.sh).
#
# One literal substring per line. If an added line in a commit/push contains
# any of these substrings, that line is skipped entirely by the scanner. Use it
# for genuine false positives only — e.g. a documentation example that happens
# to look like a private IP or a credential.
#
# '#' lines and blank lines are ignored. Keep this list short and reviewed:
# every entry is a hole in the net.
#
# (none yet)
